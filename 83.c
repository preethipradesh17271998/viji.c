#include<stdio.h>
void main()
{
    int a,b,i,c,d;
        printf("enter the value");
        scanf("%d%d",&a,&b);
        printf("enter the value");
        scanf("%d%d",&c,&d);
        printf("the modulo is %d",(c%d));
        if(a>b)
        {
        printf("the division is %d",(a/b));
        }
        else
        {
        	printf("the division is %d",(b/a));
        }
        
            
        }
